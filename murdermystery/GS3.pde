
/*class GS3 extends Gamestate {
  GS3(Player playerIn, ArrayList<Dialogue> dialogueArrayListIn) {
    super(playerIn, dialogueArrayListIn);
    
    mcClone = new NPC("mcClone",  null, null,new PVector(width/2, height/2), 25, 25, 255);//mid
    /*Page k1 = new Page("A keycard with the room A2 on it");
    ArrayList<Page> keyCardArray = new ArrayList<Page>();
    keyCardArray.add(k1);
    keyCard = new Book(keyCardArray, new PVector(width*2/3, height/2));*/
    /*
    keycard = new NPC("xRECT", null, null, new PVector(width*2/3, height/2), 1, 4, #8B5151); //doesn't sp
    hotelRoom1 = new Door(new PVector(width*.2, height*.02), 5, "H");
    hotelRoom2 = new Door(new PVector(width/2, height*.02), 5, "H");
    hotelRoom3 = new Door(new PVector(width*.8, height*.02), 5, "H");

    centerGS3Room = new Room("Hotel Rooms", null, null, null, null, null);

    centerGS3Room.objects.add(keycard);
    centerGS3Room.objects.add(hotelRoom1);
    centerGS3Room.objects.add(hotelRoom2);
    centerGS23Room.objects.add(hotelRoom3);
   
    rooms.add(centerGS2Room);

    currentRoom = centerGS2Room;
  }

  void display() {
    aDisplay();
  }


  void aDisplay() {
    currentRoom.displayRoom();
    
    p.display();
    openIt();

    textSize(25);
    talkDisplay();
    displayThingsToSee();
    
    if(keycard.beenClicked) centerGS2Room.objects.remove(keycard);
  }

  int peopleTalked;
}
*/
